function Y = bound(pa,pb,pc)
Y = (((4*dep(pc))/(dep(pa)*dep(pb)))^(1/4)); %this function computes the Cygan distance bound between
end                                          % the points A_a^-1(infinity) and \gamma(p_b) 
                                             %(or whichever case we are in)
